{
  "compilerOptions": {
    "baseUrl": ".",  // This sets the base directory for non-relative imports
    "paths": {
      "@/src/*": ["src/*"],  // Corrects the path for src
      "@/*": ["src/*"],  // Adds an alias directly to the src directory
      "contentlayer/generated": [".contentlayer/generated"]
    }
  },
  "include": [
    "src/**/*"
  ]
}