{
  "@Options": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"], // Ensures anything under src can be referenced from "@/"
    // Allows imports like "@/components/About/InsightRoll" to resolve correctly.
    }
  },
  "include": [
    "src/**/*"
  ]
}